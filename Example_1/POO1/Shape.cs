namespace POO1;

internal abstract class Shape //clase abstract no puede ser instanciada
{
    public abstract double CalculateArea(); // tiene interfaz con el metodo calcular area
                                            // tiene el metodo abstracto (no puede ser implentado, pero si sus clases hijas)
}
